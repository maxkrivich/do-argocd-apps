apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: foo-api
  labels:
    name: foo-api
spec:
  selector:
    matchLabels:
      app: foo-api
  endpoints:
  - port: http
    interval: 15s
